import 'package:flutter/material.dart';

class UbdateProudctscreen extends StatelessWidget {
  const UbdateProudctscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('update proudct'), centerTitle: true),
    );
  }
}
